# Contributing

The trickiest part of this codebase is usually the animation logic. If you're changing how an algorithm is visualized, include a description of what the visual change looks like.

```bash
npm install
npm run dev
```

Algorithm correctness matters more than speed — make sure the visualization is actually showing the right thing.
